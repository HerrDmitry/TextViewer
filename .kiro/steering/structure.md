# Project Structure

```
TextViewer/
├── TextViewer.slnx             # XML solution file (Rider/VS — references .csproj + test project)
├── Program.cs                  # .NET entry point — Photino, MessageBus, handlers, session mgmt
├── App.razor                   # Blazor root component (mounted into #app in index.html)
├── TextViewer.csproj           # MSBuild project file (.NET 10, Photino.Blazor, ng build target)
├── AGENTS.MD                   # Agent behavior rules
├── Services/                   # Backend service classes
│   ├── FileIndex.cs            # Two-phase scanner (Quick + Full)
│   ├── FileViewService.cs      # Rectangular/wrapped text view extraction
│   ├── LineIndex.cs            # Per-line length metadata (segmented, tiered)
│   ├── Segment.cs              # Fixed-size line-length storage block
│   ├── SegmentDirectory.cs     # Segment lookup + append
│   ├── IntegerTier.cs          # Compact integer width tiers (1/2/4/8 byte)
│   ├── MessageBusHost.cs       # .NET-side message bus (handler registry, envelope protocol)
│   ├── MessageProtocol.cs      # Envelope encode/decode
│   ├── PhotinoMessageBridge.cs # IMessageBridge impl for Photino window
│   ├── IMessageBridge.cs       # Bridge abstraction
│   ├── Result.cs               # Result<T,E> pattern
│   ├── ScanState.cs            # Scan state enum
│   ├── ViewError.cs            # View error types
│   ├── ViewResult.cs           # Non-wrapped view result (Rows + LineNumbers)
│   └── WrappedViewResult.cs    # Wrapped view result (Content + LineNumbers)
├── TextViewer.Tests/           # xUnit + FsCheck test project
│   ├── TextViewer.Tests.csproj
│   ├── BackendHandlerTests.cs
│   ├── MessageBusHostTests.cs
│   ├── MessageBusHostPropertyTests.cs
│   ├── MessageProtocolPropertyTests.cs
│   ├── MessageProtocolValidationPropertyTests.cs
│   ├── SessionLifecyclePropertyTests.cs
│   ├── WrappedLineCountPropertyTests.cs
│   └── Services/               # Service-level property tests
│       └── WrappedLineCountCharFallbackPropertyTests.cs
├── wwwroot/                    # Build output (generated by ng build — do not edit except index.html)
│   ├── index.html              # Host HTML page (source of truth, hand-authored)
│   ├── main.js                 # Angular CLI bundle output
│   ├── polyfills.js            # Angular polyfills bundle
│   └── styles.css              # Compiled styles
├── ClientApp/                  # Frontend source
│   ├── package.json            # npm deps (exact pinned versions)
│   ├── .npmrc                  # save-exact=true, no analytics
│   ├── angular.json            # Angular CLI build config
│   ├── tsconfig.json           # TypeScript config
│   ├── jest.config.js          # Jest test runner config
│   ├── scripts/
│   │   └── check-version-policy.mjs  # CI version gap enforcement
│   └── src/
│       ├── main.ts             # Angular bootstrap entry point
│       ├── styles.css          # Global styles
│       └── app/
│           ├── app.component.ts/html/css  # Root component (CSS Grid shell)
│           ├── app.component.spec.ts
│           ├── app.component.property.spec.ts
│           ├── open-file-integration.spec.ts
│           ├── services/
│           │   ├── message-bus-client.service.ts   # Frontend message bus client
│           │   ├── message-bus.types.ts            # Subscription/message types
│           │   ├── message-protocol.ts             # Envelope encode/decode (TS)
│           │   ├── message-bus-client.service.spec.ts
│           │   ├── message-bus-client.property.spec.ts
│           │   ├── message-bus-client-inbound.property.spec.ts
│           │   ├── message-protocol.spec.ts
│           │   └── message-protocol.property.spec.ts
│           └── shell/
│               ├── shell-state.service.ts          # Singleton state (signals, handlers, scroll)
│               ├── shell.types.ts                  # Tab, TabViewState, ScrollbarState, etc.
│               ├── extract-file-name.ts            # Path → filename util
│               ├── line-wrap-utils.ts              # splitIntoVisualRows, computeGutterWidth, etc.
│               ├── shell-state.service.spec.ts
│               ├── shell-state.property.spec.ts
│               ├── shell-state.preservation.property.spec.ts
│               ├── shell-state.line-numbers.property.spec.ts
│               ├── shell-state.scroll-navigation.property.spec.ts
│               ├── shell-state.scroll-navigation.spec.ts
│               ├── shell-state.scrollbar.spec.ts
│               ├── shell-state.text-handling.spec.ts
│               ├── shell-state.wrap-mode.property.spec.ts
│               ├── shell-state.wrapped-line-count.property.spec.ts
│               ├── shell-state.bug-condition.property.spec.ts
│               ├── text-handling.property.spec.ts
│               ├── extract-file-name.property.spec.ts
│               ├── menu-bar/          # File menu component
│               ├── tab-container/     # Tab headers component
│               ├── text-view-area/    # Content + gutter + scrollbars component
│               └── status-bar/        # File path + wrap checkbox component
└── .kiro/
    ├── hooks/
    │   └── enforce-caveman-style.kiro.hook
    ├── steering/
    │   ├── caveman-ultra.md    # Terse communication style
    │   ├── product.md          # Product description
    │   ├── structure.md        # This file
    │   ├── task_steering.md    # Task execution output rules
    │   ├── tech.md             # Tech stack reference
    │   ├── testing.md          # PBT iteration cap rules
    │   └── vendor.md           # npm version pinning policy
    └── specs/
        ├── _global/                    # Product-wide docs (source of truth)
        │   ├── requirements.md         # Combined product reqs (refs feature docs)
        │   ├── requirements-shared.md  # Infra/platform context
        │   ├── requirements-file-index.md
        │   ├── requirements-file-view-service.md
        │   ├── requirements-viewer-ui-shell.md
        │   ├── requirements-bus-service.md
        │   ├── requirements-text-handling.md
        │   ├── requirements-scroll-navigation.md
        │   ├── requirements-line-wrap-numbers.md
        │   ├── requirements-wrapped-line-count.md
        │   ├── design.md              # Combined product design (refs feature docs)
        │   ├── design-shared.md       # Arch/patterns context
        │   ├── design-file-index.md
        │   ├── design-file-view-service.md
        │   ├── design-viewer-ui-shell.md
        │   ├── design-bus-service.md
        │   ├── design-text-handling.md
        │   ├── design-scroll-navigation.md
        │   ├── design-line-wrap-numbers.md
        │   └── design-wrapped-line-count.md
        ├── file-index/                 # Feature spec snapshot
        ├── file-view-service/          # Feature spec snapshot
        ├── message-bus-service/        # Feature spec snapshot
        ├── open-file-dialog/           # Feature spec snapshot
        ├── text-handling/              # Feature spec snapshot
        ├── text-handling-more/         # Feature spec snapshot
        ├── viewer-app/                 # Feature spec snapshot
        ├── viewer-ui-shell/            # Feature spec snapshot
        ├── line-wrap-numbers/          # Feature spec snapshot
        ├── line-wrap-numbers-update/   # Bugfix spec snapshot
        ├── lineindex-max-caching/      # Bugfix spec snapshot
        ├── get-byte-offset-improvement/ # Bugfix spec snapshot
        └── wrapped-line-count/         # Feature spec snapshot
```

## Conventions

- **Single-project structure**: .NET host + frontend source in one repo
- **wwwroot/ is generated**: Angular CLI output lands here. Only `index.html` is hand-authored.
- **MSBuild orchestrates frontend build**: `.csproj` `BuildAngular` target runs `npx ng build` before compile
- **Program.cs uses class with Main**: `[STAThread] public static void Main(string[] args)` (required by Photino)
- **Angular CLI builds to IIFE-equivalent**: Output is standard ES module chunks, loaded via script tags
- **Embedded resources**: All `wwwroot/**` embedded in assembly for single-file deployment
- **No vendored binaries**: All tooling via npm
- **Services/ folder**: All backend service classes (FileIndex, FileViewService, MessageBus, etc.)
- **TextViewer.Tests/**: Separate xUnit project with FsCheck property tests

## Spec Structure Conventions

- **`_global/requirements-shared.md`**: Infra/platform context (tech stack, deploy model, communication model, glossary). Referenced by ALL feature specs and global docs.
- **`_global/design-shared.md`**: Architecture layers, build pipeline, error handling patterns, design conventions. Referenced by ALL feature specs and global docs.
- **`_global/requirements.md`**: Full product requirements (all shipped features combined). Updated when a feature ships. References feature-specific docs.
- **`_global/design.md`**: Full product design (all shipped features combined). Updated when a feature ships. References feature-specific docs.
- **`_global/requirements-{feature}.md`**: Canonical detailed requirements for a shipped feature. Referenced by both `_global/requirements.md` and the feature spec.
- **`_global/design-{feature}.md`**: Canonical detailed design for a shipped feature. Referenced by both `_global/design.md` and the feature spec.

### New Feature Spec Template

Every new feature spec MUST include these refs at the top of its files:

**`requirements.md`:**
```
#[[file:.kiro/specs/_global/requirements-shared.md]]
```

**`design.md`:**
```
#[[file:.kiro/specs/_global/design-shared.md]]
```

Feature specs contain ONLY feature-specific behavioral requirements and design. Do NOT duplicate infra/platform content from shared files.

### Shipped Feature Merge Pattern

When a feature ships, its detailed reqs/design move to `_global/requirements-{feature}.md` and `_global/design-{feature}.md`. The feature spec folder stays as snapshot of the feature development even if in future it becomes obsolete. Main source of truth will remain in _global and will change over time.

### Lifecycle

1. Create feature folder in `.kiro/specs/<feature-name>/`
2. Add `requirements.md` with shared ref + feature-only reqs
3. Add `design.md` with shared ref + feature-only design
4. Add `tasks.md` with implementation tasks
5. When feature ships → extract to `_global/requirements-{feature}.md` + `_global/design-{feature}.md`, add summary + ref in combined global docs, thin-wrap feature spec
